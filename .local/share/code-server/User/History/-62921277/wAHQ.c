#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int ab, bc, p, A, xq;
    scanf("%d", ab); scanf("%d", bc);
    p=(2*ab)+bc;
    p=p/2;
    xq=pow((p-ab),2)
    A=sqrt((p*xq))
    return 0;
}